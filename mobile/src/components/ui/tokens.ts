export const UI = {
  /** Espaciado base (multiplica por 4px) */
  space: {
    xxs: 4,
    xs: 8,
    sm: 12,
    md: 16,
    lg: 20,
    xl: 24,
  },
  radius: {
    sm: 12,
    md: 16,
    lg: 20,
    pill: 999,
  },
  /** HitSlop estándar para íconos (mejor accesibilidad) */
  hitSlop: { top: 12, bottom: 12, left: 12, right: 12 } as const,
} as const;


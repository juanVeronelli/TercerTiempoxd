# Copia a .env y ajusta. Docker Compose lee estas variables.

# --- Obligatorio: base de datos (Railway u otro) ---
# Pega aquí la connection string que te da Railway (Variables → DATABASE_URL)
DATABASE_URL=postgresql://usuario:password@containers-us-west-xxx.railway.app:5432/railway

# --- API ---
API_PORT=3000

# --- Backend (obligatorio en producción) ---
JWT_SECRET=genera-un-secreto-largo-y-aleatorio

# --- Opcionales (emails, Cloudinary, RevenueCat) ---
RESEND_API_KEY=
EMAIL_FROM=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
REVENUECAT_WEBHOOK_AUTHORIZATION=

# --- Solo si usas PostgreSQL en Docker: docker compose --profile db up -d ---
# POSTGRES_USER=tercertiempo
# POSTGRES_PASSWORD=tercertiempo_secret
# POSTGRES_DB=tercertiempo
# POSTGRES_PORT=5432
